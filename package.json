{
  "name": "server-html",
  "version": "1.0.14",
  "description": "A lightweight HTML parser with partial DOM and helper methods for working with HTML pages on your server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "html2json": "^1.0.2"
  }
}
